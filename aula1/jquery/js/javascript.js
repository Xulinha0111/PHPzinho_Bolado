//window.onload = iniciar; //Busca a função iniciar, e executa o bloco de códigos que está entre as {}

$(document).ready(iniciar);

function iniciar() {
    alert("Primeiro script utilizando jQuery")
}