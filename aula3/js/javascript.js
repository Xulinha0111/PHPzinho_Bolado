$(document).ready(iniciar)

function iniciar() {
    $("*").css("color", "blue"); // "*" --> setor global, deixa todos os elementos do site com a mesma cor, nesse caso, azul
    $("p1").css("border", "1px solid")
}